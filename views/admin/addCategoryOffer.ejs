<%- include("../partials/admin/header.ejs") %>

<div class="container mt-5">
    <h2 class="text-center mb-4">Add Category Offer</h2>

    <form action="/admin/addCategoryOffer" method="POST">
        <!-- Offer Name Field -->
        <div class="mb-3">
            <label for="offerName" class="form-label">Offer Name</label>
            <input type="text" class="form-control" id="offerName" name="offerName" placeholder="Enter offer name" required>
        </div>

        <!-- Select Category Dropdown -->
        <div class="mb-3">
            <label for="categoryId" class="form-label">Select Category</label>
            <select class="form-select" id="categoryId" name="categoryId" required>
                <option value="" disabled selected>Select a category</option>
                <% categories.forEach(category => { %>
                    <option value="<%= category._id %>"><%= category.name %></option>
                <% }) %>
            </select>
        </div>

        <!-- Offer Percentage Field -->
        <div class="mb-3">
            <label for="offerPercentage" class="form-label">Offer Percentage</label>
            <input type="number" class="form-control" id="offerPercentage" name="offerPercentage" min="0" max="100" value="0" required>
            <small class="form-text text-muted">Enter a value between 0 and 100.</small>
        </div>

        <!-- Submit and Cancel Buttons -->
        <div class="d-flex justify-content-between">
            <button type="submit" class="btn btn-primary">Add Offer</button>
            <a href="/admin/categoryOffers" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

<%- include("../partials/admin/footer.ejs") %>
